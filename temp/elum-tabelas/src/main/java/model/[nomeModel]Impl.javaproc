//codegen-ignore

package model;

/*
 * Classe de de implementações específícas para a tabela ${root.model.nomeMin}s
 *
 * @author Haftware S.I.
 */
public abstract class ${root.model.nome}Impl {

    private ${root.model.nome} self;

    public ${root.model.nome}Impl (){
        this.self = (${root.model.nome}) this;
    }

    @Override
    public String toString() {
        return self.toString();
    }

}
